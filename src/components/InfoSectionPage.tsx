import React from 'react';
import { Box, Typography } from '@mui/material';

const InfoSectionPage: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Vision & Mission</Typography>
      <Typography variant="body1">Spiritual transformation and community development.</Typography>
    </Box>
  );
};

export default InfoSectionPage;
