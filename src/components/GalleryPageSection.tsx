import React from 'react';
import { Box, Typography } from '@mui/material';

const GalleryPageSection: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Gallery Overview</Typography>
    </Box>
  );
};

export default GalleryPageSection;
