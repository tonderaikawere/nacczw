import React from 'react';
import { Box, Typography } from '@mui/material';

const GetStartedSection: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Get Started with NACC</Typography>
      <Typography variant="body1">Welcome to the National Association of Christian Churches!</Typography>
    </Box>
  );
};

export default GetStartedSection;
